# Get classes names and colors for proposed classifications

Get classes names and colors for proposed classifications

## Usage

``` r
params_classes_colors()
```

## Value

list with classes names and colors, identifiable by each
classification-name

## Examples

``` r
params_classes_colors()$class_habitat
#> Error in params_classes_colors(): could not find function "params_classes_colors"
```
