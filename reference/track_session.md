# Track input user session id and datetime.

Track input user session id and datetime.

## Usage

``` r
track_session(session = session)
```

## Arguments

- session:

  reactivesValues Shiny input.

## Value

message with the session info name and value as json format.
