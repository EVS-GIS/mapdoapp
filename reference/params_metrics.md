# Get Metric parameters

This function returns a nested list of metrics, their names and titles
used for plots as well as their description. Can for example be used for
the creation of a selectInput of metrics.

## Usage

``` r
params_metrics()
```

## Value

A table of all metrics and corresponding info

## Examples

``` r
metric_choices <- params_metrics()
```
